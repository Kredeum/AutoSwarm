{
  "name": "autoswarm-backend",
  "version": "0.0.2",
  "main": "",
  "scripts": {
    "all": "pnpm clean && pnpm format  && pnpm check && pnpm build && pnpm test && pnpm run deploy",
    "clean": "forge clean",
    "format": "forge fmt",
    "check": "echo contracts check empty",
    "build": "forge build",
    "test": "forge test",
    "deploy": "forge script script/DeployAll.s.sol",
    "broadcast": "forge script script/DeployCounter.s.sol --private-key $PRIVATE_KEY --rpc-url $CHAIN_NAME --broadcast --verify"
  },
  "keywords": [
    "forge",
    "deployment",
    "addresses"
  ],
  "author": "Kredeum",
  "license": "MIT"
}